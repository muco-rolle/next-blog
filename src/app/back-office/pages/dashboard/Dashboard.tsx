import React from 'react';
import { BackLayout } from '@components';

export const DashboardPage = () => {
    return (
        <BackLayout>
            <h1>Dashboard</h1>
        </BackLayout>
    );
};
