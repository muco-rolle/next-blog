import React from 'react';
import { BackLayout } from '@components';

export const CreatePostPage = () => {
    return (
        <BackLayout>
            <h1>Create Post</h1>
        </BackLayout>
    );
};
